## 명령모드 -> 입력모드
    i (현재위치 입력)
    a (다음위치 입력)
    o (다음행에 입력)
    h,j,k,l (커서 이동)
    w (단어 이동)
    cw (단어 변환)
    x (1글자 삭제)
    dd (1행 삭제)
    /
    :w (저장)
    :wq! (저장 & 종료)
    G (문서 제일 마지막)
    :라인넘버 (해당 라인으로 이동)
    :sh (쉘 명령어 사용, EXIT로 복귀)
    :set nu
    :set nonu
## 입력모드 -> 명령모드
    esc
    :q
    :q! (저장x, 강제종료)
